interface:
  display_name: "Writing Editor"
  short_description: "Edit and finalize pending writing."
  default_prompt: "Use $writing-editor to refine a draft in writings/pending/ and prepare it for writings/final/."

policy:
  allow_implicit_invocation: true
